---
interface Props {
  title: string;
  subtitle?: string;
  /** Body paragraph below subtitle. */
  body?: string;
  conferenceName?: string;
  date?: string;
  /** Primary CTA URL (e.g. training signup). If set, the button is shown. */
  primaryCtaUrl?: string;
  /** Label for the primary CTA button. */
  primaryCtaLabel?: string;
}

const { title, subtitle, body, conferenceName, date, primaryCtaUrl = '#', primaryCtaLabel = 'Sign up' } = Astro.props;
---

<section class="relative bg-via-black text-white overflow-hidden">
  <div class="relative z-10 mx-auto max-w-6xl px-6 py-24 sm:py-32 lg:py-40">
    <div class="mx-auto max-w-3xl text-center">
      <h1 class="text-4xl font-bold tracking-tight sm:text-5xl lg:text-6xl text-white uppercase tracking-wider" style="text-transform: uppercase;">
        {title}
      </h1>
      {subtitle && (
        <p class="mt-6 text-lg sm:text-xl text-slate-300 font-semibold">
          {subtitle}
        </p>
      )}
      {body && (
        <p class="mt-4 text-base sm:text-lg text-slate-400">
          {body}
        </p>
      )}
      {(conferenceName || date) && (
        <p class="mt-4 text-base sm:text-lg text-slate-400">
          {conferenceName}
          {conferenceName && date ? ' · ' : ''}
          {date}
        </p>
      )}
      {primaryCtaUrl && (
        <div class="mt-10 flex justify-center">
          <a
            href={primaryCtaUrl}
            class="inline-flex items-center rounded-lg bg-white px-6 py-3 text-base font-semibold text-via-black shadow-sm transition hover:bg-slate-100 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
          >
            {primaryCtaLabel}
          </a>
        </div>
      )}
    </div>
  </div>
</section>
